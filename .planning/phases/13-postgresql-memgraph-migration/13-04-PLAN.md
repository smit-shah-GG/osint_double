---
phase: 13-postgresql-memgraph-migration
plan: 04
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - osint_system/data_management/article_store.py
  - osint_system/data_management/fact_store.py
autonomous: true

must_haves:
  truths:
    - "ArticleStore methods return identical dict shapes as the in-memory implementation"
    - "FactStore methods return identical dict shapes as the in-memory implementation"
    - "Both stores use AsyncSession per method call (no shared sessions)"
    - "Existing pipeline and API code calling ArticleStore/FactStore runs without modification"
  artifacts:
    - path: "osint_system/data_management/article_store.py"
      provides: "PostgreSQL-backed ArticleStore"
      contains: "async_sessionmaker"
    - path: "osint_system/data_management/fact_store.py"
      provides: "PostgreSQL-backed FactStore"
      contains: "async_sessionmaker"
  key_links:
    - from: "osint_system/data_management/article_store.py"
      to: "osint_system/data_management/models/article.py"
      via: "imports ArticleModel for ORM operations"
      pattern: "from.*models.*import.*ArticleModel"
    - from: "osint_system/data_management/fact_store.py"
      to: "osint_system/data_management/models/fact.py"
      via: "imports FactModel for ORM operations"
      pattern: "from.*models.*import.*FactModel"
    - from: "osint_system/data_management/article_store.py"
      to: "osint_system/data_management/database.py"
      via: "uses session factory for DB access"
      pattern: "session_factory"
---

<objective>
Migrate ArticleStore and FactStore from in-memory+JSON to SQLAlchemy+PostgreSQL backends while preserving identical method signatures and return types.

Purpose: These are the two largest and most complex stores (426 and 693 lines respectively). They handle article persistence and fact storage with deduplication, variant linking, and indexed lookups. The migration swaps the dict+lock backend for SQLAlchemy async sessions while keeping every public method signature and return type identical.

Output: Rewritten article_store.py and fact_store.py with PostgreSQL backends
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-postgresql-memgraph-migration/13-CONTEXT.md
@.planning/phases/13-postgresql-memgraph-migration/13-RESEARCH.md
@.planning/phases/13-postgresql-memgraph-migration/13-02-SUMMARY.md
@osint_system/data_management/article_store.py
@osint_system/data_management/fact_store.py
@osint_system/data_management/models/article.py
@osint_system/data_management/models/fact.py
@osint_system/data_management/database.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ArticleStore to PostgreSQL</name>
  <files>osint_system/data_management/article_store.py</files>
  <action>
    Rewrite ArticleStore to use SQLAlchemy async sessions instead of in-memory dicts.

    **Constructor changes:**
    - Remove `persistence_path` parameter
    - Add `session_factory: async_sessionmaker[AsyncSession]` parameter
    - Store as `self._session_factory`
    - Remove `self._storage`, `self._url_index`, `self._lock`, `self._persistence_path`
    - Remove `_save_to_file`, `_load_from_file` methods entirely

    **Method migration pattern** (apply to EVERY public method):
    - Replace `async with self._lock:` with `async with self._session_factory() as session:`
    - Replace dict lookups with SQLAlchemy queries using `select()`, `where()`, etc.
    - Replace dict mutations with `session.add()`, `session.merge()`, `session.delete()`
    - Wrap mutations in `async with session.begin():` for auto-commit
    - Convert ORM results to dicts using `ArticleModel.to_dict()` before returning

    **Key methods to migrate** (read existing file to understand ALL methods):
    - `save_articles(investigation_id, articles, metadata)` -> INSERT via ArticleModel.from_dict(), skip duplicates by URL
    - `get_articles(investigation_id)` -> SELECT WHERE investigation_id, return list of dicts
    - `get_article_by_url(investigation_id, url)` -> SELECT WHERE url
    - `get_article_count(investigation_id)` -> SELECT COUNT
    - `get_all_investigations()` -> SELECT DISTINCT investigation_id
    - `delete_investigation(investigation_id)` -> DELETE WHERE investigation_id
    - Any other public methods that exist

    **Critical contract:** Every method must return the EXACT same types and dict shapes as the current implementation. Callers must not know the backend changed. Do NOT add new methods or change signatures.

    **URL dedup:** The existing store uses `_url_index` for O(1) URL dedup. Replace with a UNIQUE constraint or WHERE NOT EXISTS subquery on (investigation_id, url). ArticleModel should have a unique constraint on (investigation_id, url) or use INSERT ... ON CONFLICT DO NOTHING.

    **Remove JSON persistence:** Delete all `_save_to_file`, `_load_from_file`, and file I/O code. PostgreSQL is the single source of truth.
  </action>
  <verify>
    - `uv run python -c "from osint_system.data_management.article_store import ArticleStore; print('ArticleStore imports ok')"` succeeds
    - `uv run python -c "import inspect; from osint_system.data_management.article_store import ArticleStore; methods = [m for m in dir(ArticleStore) if not m.startswith('_')]; print(f'Public methods: {methods}')"` shows same methods as before
    - No references to `_save_to_file`, `_load_from_file`, `json.dump`, `json.load` in the file
  </verify>
  <done>
    ArticleStore uses SQLAlchemy async sessions. All public methods preserved with identical signatures and return types. JSON persistence removed. URL deduplication via database constraints.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate FactStore to PostgreSQL</name>
  <files>osint_system/data_management/fact_store.py</files>
  <action>
    Rewrite FactStore to use SQLAlchemy async sessions. This is the most complex store (693 lines) with multiple indexes and variant linking.

    **Constructor changes:**
    - Remove `persistence_path` parameter
    - Add `session_factory: async_sessionmaker[AsyncSession]` parameter
    - Store as `self._session_factory`
    - Remove `self._storage`, `self._fact_index`, `self._hash_index`, `self._source_index`, `self._lock`, `self._persistence_path`
    - Remove `_save_to_file`, `_load_from_file` methods

    **Index replacement:**
    - `_fact_index` (fact_id -> fact) becomes: `SELECT WHERE fact_id = $id`
    - `_hash_index` (content_hash -> list[fact_id]) becomes: `SELECT WHERE content_hash = $hash`
    - `_source_index` (source_id -> list[fact_id]) becomes: JSONB query on provenance or a join

    **Key methods to migrate** (read existing file for ALL methods):
    - `save_facts(investigation_id, facts, investigation_metadata)` -> bulk INSERT via FactModel.from_dict(). Return dict with saved/updated/skipped/total counts. Skip duplicates by content_hash.
    - `get_facts(investigation_id)` -> SELECT WHERE investigation_id, return dict of {fact_id: fact_dict}
    - `get_fact(investigation_id, fact_id)` -> SELECT WHERE fact_id
    - `get_fact_by_id(fact_id)` -> SELECT WHERE fact_id (cross-investigation lookup)
    - `get_facts_by_hash(content_hash)` -> SELECT WHERE content_hash
    - `link_variants(investigation_id, primary_id, variant_id)` -> UPDATE variants JSONB array
    - `get_fact_count(investigation_id)` -> SELECT COUNT
    - `update_fact(investigation_id, fact_id, updates)` -> UPDATE specific columns/JSONB
    - `delete_investigation(investigation_id)` -> DELETE WHERE investigation_id
    - Any other public methods

    **Variant linking:** The existing store appends variant_id to a list in the primary fact's `variants` field. In PostgreSQL, this is a JSONB array append operation. Use `func.jsonb_insert` or read-modify-write within a transaction.

    **Critical contract:** Return types must match exactly. `get_facts()` returns `Dict[str, Dict]` keyed by fact_id. `save_facts()` returns the stats dict.

    **Remove JSON persistence:** Delete all file I/O code.
  </action>
  <verify>
    - `uv run python -c "from osint_system.data_management.fact_store import FactStore; print('FactStore imports ok')"` succeeds
    - `uv run python -c "import inspect; from osint_system.data_management.fact_store import FactStore; methods = [m for m in dir(FactStore) if not m.startswith('_')]; print(f'Public methods: {methods}')"` shows same methods as before
    - No references to `_save_to_file`, `_load_from_file`, `json.dump`, `json.load` in the file
    - `grep -c 'self._lock' osint_system/data_management/fact_store.py` returns 0 (no asyncio.Lock)
    - `grep -c 'session_factory' osint_system/data_management/fact_store.py` returns >0
  </verify>
  <done>
    FactStore uses SQLAlchemy async sessions. All public methods preserved with identical signatures and return types. Content hash dedup, variant linking, and all indexed lookups work via SQL queries. JSON persistence removed.
  </done>
</task>

</tasks>

<verification>
- Both stores import and instantiate cleanly with a session factory
- Public method signatures unchanged from original implementations
- No asyncio.Lock, no dict-based storage, no JSON file I/O in either file
- Both stores use async with session_factory() pattern per method call
</verification>

<success_criteria>
ArticleStore and FactStore operate against PostgreSQL with zero interface changes. Any code that calls these stores works identically whether it was using the old in-memory backend or the new PostgreSQL backend.
</success_criteria>

<output>
After completion, create `.planning/phases/13-postgresql-memgraph-migration/13-04-SUMMARY.md`
</output>

---
phase: 01-foundation-environment
plan: 01
type: execute
---

<objective>
Set up Python environment with uv and establish project structure.

Purpose: Create a fast, reproducible development environment that will scale with the OSINT system.
Output: Working Python project with uv management, proper structure, and core dependencies installed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-environment/01-CONTEXT.md
@.planning/phases/01-foundation-environment/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install uv and initialize Python project</name>
  <files>pyproject.toml, .gitignore, .python-version, README.md</files>
  <action>
    Install uv globally if not present (curl -LsSf https://astral.sh/uv/install.sh | sh).
    Initialize new Python project with: uv init osint_system --python 3.11.
    This creates pyproject.toml with proper structure.
    Update pyproject.toml to include project metadata: name="osint_system", version="0.1.0", description="LLM-powered multi-agent OSINT system".
    Create .python-version with "3.11" for IDE detection.
    Update .gitignore to include .venv/, __pycache__/, *.pyc, .env, .DS_Store, logs/, *.log.
    Update README.md with basic project description and setup instructions.
  </action>
  <verify>uv --version returns version, pyproject.toml exists with correct metadata, .venv directory created</verify>
  <done>Project initialized with uv, virtual environment active, pyproject.toml configured</done>
</task>

<task type="auto">
  <name>Task 2: Install core dependencies with lockfile</name>
  <files>pyproject.toml, uv.lock</files>
  <action>
    Use uv to add core dependencies:
    uv add langchain langgraph google-generativeai loguru typer pydantic pydantic-settings mcp rich python-dotenv
    Add dev dependencies:
    uv add --dev pytest pytest-asyncio pytest-cov mypy ruff
    This will create uv.lock for reproducible installs.
    Verify all packages installed correctly with: uv pip list.
    The lockfile ensures exact versions across all environments.
  </action>
  <verify>uv.lock exists, uv pip list shows all packages, uv run python -c "import langchain; import loguru" succeeds</verify>
  <done>All core dependencies installed with lockfile, imports work correctly</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `uv pip list` shows all required packages
- [ ] `uv run python -c "import langchain; import langgraph; import loguru"` succeeds
- [ ] uv.lock file exists for reproducible installs
- [ ] .venv directory is present and active
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No import errors for core packages
- Project structure follows Python best practices
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-environment/01-01-SUMMARY.md`:

# Phase 1 Plan 1: Environment & Project Setup Summary

**Established Python project with uv package manager and core dependencies.**

## Accomplishments

- Installed uv for 10-100x faster package management
- Created project structure with pyproject.toml
- Installed all core dependencies with lockfile

## Files Created/Modified

- `pyproject.toml` - Project configuration with dependencies
- `uv.lock` - Lockfile for reproducible installs
- `.python-version` - Python version specification
- `.gitignore` - Ignore patterns for Python project
- `README.md` - Basic project documentation

## Decisions Made

None - followed established patterns from research.

## Issues Encountered

None - standard setup completed successfully.

## Next Step

Ready for 01-02-PLAN.md (Configuration & Logging Infrastructure)
</output>
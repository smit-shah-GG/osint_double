---
phase: 12-api-layer-pipeline-events
plan: 04
type: execute
wave: 3
depends_on: ["12-02", "12-03"]
files_modified:
  - osint_system/api/app.py
  - osint_system/serve.py
  - tests/api/test_app.py
  - tests/api/test_openapi.py
autonomous: false

must_haves:
  truths:
    - "create_api_app() returns a fully configured FastAPI app with all 6 route modules mounted, CORS middleware, RFC 7807 error handlers, and app.state initialized with event bus, registry, and store dicts"
    - "The OpenAPI spec at /openapi.json describes all endpoints with correct request/response schemas and includes SSE stream documentation"
    - "The serve.py entrypoint boots the API app with uvicorn and all endpoints are accessible"
    - "All tests across the api/ test suite pass together without import or state conflicts"
  artifacts:
    - path: "osint_system/api/app.py"
      provides: "API app factory that wires all route modules, middleware, error handlers, and app.state"
      exports: ["create_api_app"]
    - path: "osint_system/serve.py"
      provides: "Updated entrypoint that boots the API app (alongside or replacing HTMX dashboard)"
      contains: "create_api_app"
  key_links:
    - from: "osint_system/api/app.py"
      to: "osint_system/api/routes/investigations.py"
      via: "app.include_router(investigations.router)"
      pattern: "include_router"
    - from: "osint_system/api/app.py"
      to: "osint_system/api/routes/stream.py"
      via: "app.include_router(stream.router)"
      pattern: "include_router"
    - from: "osint_system/api/app.py"
      to: "osint_system/api/routes/facts.py"
      via: "app.include_router(facts.router)"
      pattern: "include_router"
    - from: "osint_system/api/app.py"
      to: "osint_system/api/routes/reports.py"
      via: "app.include_router(reports.router)"
      pattern: "include_router"
    - from: "osint_system/api/app.py"
      to: "osint_system/api/routes/sources.py"
      via: "app.include_router(sources.router)"
      pattern: "include_router"
    - from: "osint_system/api/app.py"
      to: "osint_system/api/routes/graph.py"
      via: "app.include_router(graph.router)"
      pattern: "include_router"
    - from: "osint_system/api/app.py"
      to: "osint_system/api/errors.py"
      via: "register_error_handlers(app)"
      pattern: "register_error_handlers"
    - from: "osint_system/api/app.py"
      to: "starlette.middleware.cors"
      via: "CORSMiddleware for frontend dev server"
      pattern: "CORSMiddleware"
---

<objective>
Wire all route modules into a single FastAPI app factory, configure CORS + error handling, update the serve.py entrypoint, validate the OpenAPI spec, and run the full test suite. This plan integrates all Phase 12 work into a bootable, testable API server.

Purpose: Individual route modules are useless without an app factory that mounts them, configures middleware, initializes app.state, and provides the OpenAPI spec. This plan is the integration point that makes the API real.
Output: A working `create_api_app()` factory, updated serve.py, and full test suite validation including OpenAPI spec verification.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-api-layer-pipeline-events/12-CONTEXT.md
@.planning/phases/12-api-layer-pipeline-events/12-RESEARCH.md
@.planning/phases/12-api-layer-pipeline-events/12-01-SUMMARY.md
@.planning/phases/12-api-layer-pipeline-events/12-02-SUMMARY.md
@.planning/phases/12-api-layer-pipeline-events/12-03-SUMMARY.md
@osint_system/api/schemas.py
@osint_system/api/errors.py
@osint_system/api/dependencies.py
@osint_system/api/events/event_bus.py
@osint_system/api/events/investigation_registry.py
@osint_system/api/routes/investigations.py
@osint_system/api/routes/stream.py
@osint_system/api/routes/facts.py
@osint_system/api/routes/reports.py
@osint_system/api/routes/sources.py
@osint_system/api/routes/graph.py
@osint_system/serve.py
@osint_system/dashboard/app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: API app factory, serve.py update, and integration tests</name>
  <files>
    osint_system/api/app.py
    osint_system/serve.py
    tests/api/test_app.py
    tests/api/test_openapi.py
  </files>
  <action>
**app.py** -- API app factory (parallel to existing `osint_system/dashboard/app.py`):

```python
def create_api_app() -> FastAPI:
```

1. Create FastAPI instance with metadata:
   - `title="OSINT Intelligence System API"`
   - `version="2.0.0"`
   - `description="REST API for launching, monitoring, and reviewing OSINT investigations"`

2. Add CORS middleware (RESEARCH.md example):
   ```python
   app.add_middleware(
       CORSMiddleware,
       allow_origins=[
           "http://localhost:3000",   # Next.js dev server
           "http://localhost:5173",   # Vite dev server
           "http://127.0.0.1:3000",
           "http://127.0.0.1:5173",
       ],
       allow_credentials=True,
       allow_methods=["*"],
       allow_headers=["*"],
   )
   ```

3. Register RFC 7807 error handlers: `register_error_handlers(app)`

4. Initialize app.state:
   - `app.state.event_bus = PipelineEventBus()`
   - `app.state.investigation_registry = InvestigationRegistry()`
   - `app.state.active_tasks: dict[str, asyncio.Task] = {}`
   - `app.state.cancel_flags: dict[str, asyncio.Event] = {}`
   - `app.state.investigation_stores: dict[str, dict] = {}`
   - `app.state.graph_pipelines: dict = {}`

5. Include all routers:
   ```python
   from osint_system.api.routes import investigations, stream, facts, reports, sources, graph
   app.include_router(investigations.router)
   app.include_router(stream.router)
   app.include_router(facts.router)
   app.include_router(reports.router)
   app.include_router(sources.router)
   app.include_router(graph.router)
   ```

6. Add a health check endpoint: `GET /api/v1/health` returning `{"status": "ok"}`

7. Add shutdown event handler that cancels all active tasks gracefully

**serve.py** update:

Update the existing serve.py to support both modes:
- `uv run python -m osint_system.serve` (no args) -- boots the API app on port 8000 for new investigations
- `uv run python -m osint_system.serve <investigation_id>` -- boots the HTMX dashboard for viewing existing investigation data (existing behavior preserved)

For API mode (no investigation_id):
```python
from osint_system.api.app import create_api_app
app = create_api_app()
uvicorn.run(app, host="0.0.0.0", port=8000)
```

For dashboard mode (with investigation_id):
Keep existing behavior that creates stores from JSON files and boots the HTMX dashboard.

Print clear banner showing which mode is active and available endpoints.

**tests/api/test_app.py**:
- Test create_api_app returns FastAPI instance
- Test health endpoint returns 200 with {"status": "ok"}
- Test CORS headers present on OPTIONS preflight request
- Test RFC 7807 error format: trigger a 404 and verify response has type/title/status/detail/instance fields with content-type application/problem+json
- Test all route modules are mounted (verify route count or specific paths exist in app.routes)

**tests/api/test_openapi.py**:
- Test GET /openapi.json returns valid JSON
- Test schema contains paths for all expected endpoints:
  - /api/v1/investigations (GET, POST)
  - /api/v1/investigations/{investigation_id} (GET, DELETE)
  - /api/v1/investigations/{investigation_id}/cancel (POST)
  - /api/v1/investigations/{investigation_id}/regenerate (POST)
  - /api/v1/investigations/{investigation_id}/stream (GET)
  - /api/v1/investigations/{investigation_id}/facts (GET)
  - /api/v1/investigations/{investigation_id}/facts/{fact_id} (GET)
  - /api/v1/investigations/{investigation_id}/reports (GET)
  - /api/v1/investigations/{investigation_id}/reports/latest (GET)
  - /api/v1/investigations/{investigation_id}/reports/{version} (GET)
  - /api/v1/investigations/{investigation_id}/sources (GET)
  - /api/v1/investigations/{investigation_id}/graph/nodes (GET)
  - /api/v1/investigations/{investigation_id}/graph/edges (GET)
  - /api/v1/investigations/{investigation_id}/graph/query (GET)
  - /api/v1/health (GET)
- Test schema components include InvestigationResponse, FactResponse, PaginatedResponse, ProblemDetail, LaunchRequest
- Test the spec is valid (no $ref cycles, all references resolve)

Run the full test suite to confirm no conflicts:
`uv run python -m pytest tests/api/ -v`
  </action>
  <verify>
`uv run python -m pytest tests/api/ -v` -- ALL api tests pass (test_schemas, test_event_bus, test_investigation_registry, test_investigations_route, test_stream_route, test_facts_route, test_reports_route, test_sources_route, test_graph_route, test_app, test_openapi).
`uv run python -c "from osint_system.api.app import create_api_app; app = create_api_app(); print(f'Routes: {len(app.routes)}, Title: {app.title}')"` shows all routes mounted.
`uv run python -c "from osint_system.api.app import create_api_app; import json; app = create_api_app(); spec = app.openapi(); print(f'Paths: {len(spec[\"paths\"])}, Schemas: {len(spec[\"components\"][\"schemas\"])}')"` shows expected path and schema counts.
  </verify>
  <done>
API app factory creates fully configured FastAPI app with all routes, CORS, error handling, and app.state. serve.py supports both API mode (no args) and dashboard mode (with investigation_id). OpenAPI spec at /openapi.json describes all 15 endpoints with correct schemas. Full test suite passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete API layer with 15 REST endpoints + SSE streaming, RFC 7807 errors, event bus, investigation registry, and OpenAPI spec generation. Ready for frontend consumption in Phase 14.</what-built>
  <how-to-verify>
1. Start the API server: `uv run python -m osint_system.serve`
2. Verify health check: `curl http://localhost:8000/api/v1/health` -- should return `{"status":"ok"}`
3. View the OpenAPI spec: open `http://localhost:8000/docs` in a browser -- should show Swagger UI with all endpoints documented
4. Test investigation launch: `curl -X POST http://localhost:8000/api/v1/investigations -H "Content-Type: application/json" -d '{"objective":"Test investigation"}'` -- should return 202 with investigation ID and stream_url
5. Test SSE stream (if investigation was created): `curl -N http://localhost:8000/api/v1/investigations/{id}/stream` -- should receive SSE events as pipeline runs
6. Run full test suite: `uv run python -m pytest tests/api/ -v` -- all tests should pass
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `uv run python -m pytest tests/api/ -v` -- complete test suite passes
2. API server boots without errors via `uv run python -m osint_system.serve`
3. /openapi.json contains all 15 endpoint paths with correct HTTP methods
4. /docs shows interactive Swagger UI
5. POST /investigations returns 202 and actually starts pipeline
6. SSE stream delivers events during pipeline execution
7. CORS headers present for localhost:3000 origins
8. Error responses use RFC 7807 format (application/problem+json)
</verification>

<success_criteria>
- create_api_app() wires all 6 route modules + health endpoint
- CORS configured for Next.js dev server ports
- RFC 7807 error handlers registered
- serve.py supports dual mode (API + legacy dashboard)
- OpenAPI spec covers all endpoints with typed schemas
- Full test suite passes (10+ test files)
- Requirements covered: All API-01 through API-07, EVENT-01 through EVENT-03
</success_criteria>

<output>
After completion, create `.planning/phases/12-api-layer-pipeline-events/12-04-SUMMARY.md`
</output>

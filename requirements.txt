# Core dependencies (already installed)
google-genai>=1.0
pydantic>=2.0
loguru==0.7.2
click==8.1.7
python-dotenv==1.0.1

# Agent architecture dependencies
langchain
langgraph
langchain-mcp-adapters  # Provides MCP integration with LangChain tools (includes mcp package)
aiopubsub
structlog

# Additional dependencies
aiofiles
langchain-google-genai  # For Gemini integration with LangChain
ddgs>=9.0  # DuckDuckGo search for verification (no API key required)
openai>=1.0  # OpenRouter compatibility layer (optional, for quota bypass)

# Reddit crawler dependencies
asyncpraw>=7.7.0  # Async Reddit API wrapper
httpx>=0.25.0  # Fast async HTTP client
aiometer>=0.5.0  # Rate limiting for async operations
yarl>=1.9.0  # URL handling and manipulation
tenacity>=8.2.0  # Retry logic with exponential backoff

# Document crawler dependencies
pypdfium2>=4.25.0  # PDF text extraction (best quality per research)
pdfplumber>=0.10.0  # PDF table extraction fallback
trafilatura>=1.6.0  # Web content extraction (F1 score 0.958)
beautifulsoup4>=4.12.0  # HTML parsing helper

# Web scraper dependencies
playwright>=1.40.0  # Browser automation for JS-heavy sites
playwright-stealth>=2.0.2  # Anti-bot evasion for Playwright (stealth_async)

# Graph database dependencies
neo4j>=6.1.0  # Bolt driver for Memgraph (same Bolt protocol)
networkx>=3.0  # In-memory graph for tests/CI (no Docker dependency)

# Analysis & reporting engine dependencies (Phase 10)
aiosqlite>=0.22.1  # Async SQLite for investigation database export
mistune>=3.0  # Markdown -> HTML parser (CommonMark-compatible)
Jinja2>=3.1.6  # Template engine for reports and dashboard
fastapi>=0.115.0  # Async web framework for dashboard
uvicorn>=0.30.0  # ASGI server for dashboard
python-multipart>=0.0.9  # Form data parsing for FastAPI POST routes

# PostgreSQL + pgvector dependencies (Phase 13)
sqlalchemy[asyncio]>=2.0  # Async ORM for PostgreSQL stores
asyncpg>=0.29.0  # Async PostgreSQL driver (5x faster than psycopg3 async)
psycopg[binary]>=3.1  # Sync PostgreSQL driver (needed for Alembic migrations)
pgvector>=0.3.0  # pgvector SQLAlchemy integration (Vector type, HNSW indexes)
alembic>=1.12  # Schema migration management (async template)
